# 检索式时间序列预测统一框架  
## 技术白皮书 + 项目结构定义（中文）

版本：v1.0  
日期：2026-04-11  
适用对象：算法工程师、平台工程师、研究员、审稿人、技术管理者

---

## 1. 摘要

本文给出一个面向长期时间序列预测任务的“检索式统一框架”技术白皮书。框架核心思想是：不直接让模型“凭空回归未来”，而是将预测问题转化为“历史片段检索 + 候选未来聚合”的可解释流程。系统统一支持 7 类检索模型：PatternSearch、LSHSearch、SAXSearch、DTWSearch、MatrixProfileSearch、TS2VecSearch、RAGSearch。  

本文重点定义：
1. 任务形式化定义与理论边界。  
2. 7 模型的定位、适用条件、归一化策略、保留结论。  
3. 统一接口规范与工程实现规范。  
4. 防泄漏、可复现、可审计、可验收的实验流程。  
5. 标准化项目目录与模块职责。

---

## 2. 背景与目标

传统深度预测模型常存在以下问题：
1. 可解释性弱。  
2. 数据泄漏风险隐蔽。  
3. 工程审计困难。  
4. 当模式重复性强时，未充分利用“历史相似段”的先验。  

本框架目标：
1. 构建“检索式时序预测”统一技术底座。  
2. 明确分离召回、精排、聚合三阶段。  
3. 用统一评估口径比较不同检索模型。  
4. 保证无未来信息泄漏。  
5. 支持复现实验、审计追溯、论文级代码规范。

---

## 3. 任务定义（统一数学表述）

给定多变量时间序列 \(X\in\mathbb{R}^{T\times C}\)。  
对任意预测时刻 \(t\)：

1. 查询历史窗口  
\[
Q_t = X[t-\text{seq\_len}:t)
\]

2. 在训练记忆库中检索候选历史窗口  
\[
H_s = X[s-\text{seq\_len}:s)
\]

3. 取其对应未来窗口  
\[
F_s = X[s:s+\text{pred\_len})
\]

4. 通过候选聚合器得到预测  
\[
\hat{Y}_t = \text{Aggregate}\left(\{F_{s_i}\}_{i=1}^k,\{w_i\}_{i=1}^k\right)
\]

关键约束：
1. 记忆库仅由 train split 构建。  
2. 检索仅使用历史窗口 \(Q_t\)。  
3. 聚合仅使用候选未来窗口 \(F_s\)。  
4. val/test 期间不可写入记忆库。  

---

## 4. 模型家族与理论定位

### 4.1 PatternSearch
定位：精确距离检索预测器。  
适用：模式重复强、形态相近场景。  
策略：窗口级 z-norm 后 L2 检索，top-k 未来加权。  
结论：可作为独立预测器保留。

### 4.2 LSHSearch
定位：近似近邻召回器。  
适用：大规模记忆库加速。  
策略：随机投影哈希召回 + 精确重排 + 未来聚合。  
结论：保留，但必须“召回+精排”，不能哈希命中即预测。

### 4.3 SAXSearch
定位：符号化粗检索器。  
适用：粗粒度模式快速匹配。  
策略：z-norm + PAA + SAX 桶召回 + 精排 + 聚合。  
结论：保留，但应定义为 coarse recall forecaster。

### 4.4 DTWSearch
定位：弹性形状匹配预测器。  
适用：存在局部时移、速率变化的序列。  
策略：约束 DTW（Sakoe-Chiba）+ top-k 聚合。  
结论：可独立保留，但需明确复杂度边界。

### 4.5 MatrixProfileSearch
定位：z-normalized 子序列最近邻预测器。  
适用：固定长度子序列相似检索。  
说明：工程实现可为“z-norm subsequence NN”，不等价完整 motif discovery。  
结论：保留但重定义，避免“名不副实”。

### 4.6 TS2VecSearch
定位：表征检索预测器。  
适用：原始欧氏距离不足以表达语义相似时。  
策略：仅 train 自监督训练 encoder，embedding 检索 + 未来聚合。  
结论：保留，必须严格 split 隔离。

### 4.7 RAGSearch
定位：学习式精排检索预测器。  
适用：候选已有但排序能力不足。  
策略：先召回候选，再基于 history-history 打分重排，再聚合 future。  
结论：保留为 reranker，不建议黑盒端到端直接回归未来。

---

## 5. 归一化与尺度策略白皮书

核心原则：统一接口不等于统一归一化。  
每模型推荐：
1. PatternSearch：`window_zscore`。  
2. LSHSearch：`window_zscore`（稳定哈希）。  
3. SAXSearch：`window_zscore`（SAX前置条件）。  
4. DTWSearch：`window_zscore`（避免幅值主导路径代价）。  
5. MatrixProfileSearch：`window_zscore`（子序列标准定义）。  
6. TS2VecSearch：`standard`（train fit-only）+ embedding L2 norm。  
7. RAGSearch：`standard`（query/candidate一致）+ 禁止候选 future 进入 query 编码。  

评估策略：
1. 默认 inverse_transform 后计算指标。  
2. 指标维度严格对齐 `[B, pred_len, C]`。  
3. MAPE/MSPE 对零值分母做保护。

---

## 6. 统一接口规范（Framework Contract）

所有模型必须实现同一语义流程：

1. `build_memory_bank(train_histories, train_futures)`  
2. `fit(train_histories, train_futures)`  
3. `retrieve(query_histories) -> candidate_ids, scores`  
4. `rerank(query_histories, candidate_ids, scores)`  
5. `aggregate_future(candidate_futures, rerank_scores)`  
6. `forecast(query_histories) -> prediction`  

工程约束：
1. 不允许在单函数中混写“训练+建库+推理+评估”。  
2. 不允许复制粘贴各模型公共逻辑。  
3. 必须可单独审计每阶段输入输出。

---

## 7. 防泄漏与数据治理规范

必须满足：
1. 严格时间顺序切分 train/val/test。  
2. scaler 仅 train fit。  
3. memory bank 仅 train 构建。  
4. query 无 future 输入。  
5. val/test 不参与 encoder 训练。  
6. 不允许使用 query 自身 future 作为候选。  
7. 结果保存包含参数、日志、指标、预测数组，支持复核。

---

## 8. 实验规范与验收标准

### 8.1 标准数据集
1. ETTh1  
2. ETTh2  
3. ETTm1  
4. ETTm2  

### 8.2 标准配置建议
1. seq_len: 96/192/336/720  
2. pred_len: 96/192/336/720  
3. features: M  
4. top_k、recall_k、distance/normalization 策略可配置

### 8.3 统一输出格式
字段：
1. model  
2. dataset  
3. seq_len  
4. pred_len  
5. mae  
6. mse  
7. rmse  
8. mape  
9. runtime（可选）

### 8.4 验收门槛
1. 7 模型均可跑通 fit+forecast。  
2. 无泄漏测试通过。  
3. shape、指标、归一化测试通过。  
4. 日志与结果可追溯。  
5. 同配置多次运行波动可控。

---

## 9. 项目结构定义（标准版）

```text
.
├── run.py
├── exp/
│   ├── exp_basic.py
│   ├── exp_retrieval_forecasting.py
│   └── exp_long_term_forecasting.py
├── data_provider/
│   ├── data_factory.py
│   ├── data_loader.py
│   └── window_builder.py
├── models/
│   ├── base_retriever.py
│   ├── PatternSearch.py
│   ├── LSHSearch.py
│   ├── SAXSearch.py
│   ├── DTWSearch.py
│   ├── MatrixProfileSearch.py
│   ├── TS2VecSearch.py
│   ├── RAGSearch.py
│   └── __init__.py
├── retrieval/
│   ├── memory_bank.py
│   ├── distance.py
│   ├── candidate_utils.py
│   ├── rerank.py
│   ├── aggregation.py
│   └── __init__.py
├── utils/
│   ├── metrics.py
│   ├── seed.py
│   ├── logging_utils.py
│   ├── audit.py
│   ├── timefeatures.py
│   └── tools.py
├── scripts/
│   └── long_term_forecast/
│       ├── ETTh1/
│       ├── ETTh2/
│       ├── ETTm1/
│       └── ETTm2/
├── tests/
│   ├── test_data_leakage.py
│   ├── test_metrics.py
│   ├── test_shapes.py
│   ├── test_normalization.py
│   └── test_model_pipeline.py
├── reports/
│   ├── theoretical_audit_report.md
│   ├── implementation_audit_report.md
│   ├── modification_list.md
│   └── experiment_acceptance.md
└── README.md
```

---

## 10. 模块职责定义（简明）

1. `run.py`：统一 CLI、参数校验、实验调度、summary 输出。  
2. `exp/*`：训练/验证/测试生命周期管理。  
3. `data_provider/*`：数据切分、窗口构造、逆归一化。  
4. `models/*`：模型专属召回/精排逻辑，遵循统一接口。  
5. `retrieval/*`：通用检索原语与聚合原语。  
6. `utils/*`：指标、随机种子、日志、审计工具。  
7. `tests/*`：功能正确性与防泄漏保障。  
8. `reports/*`：理论与实现审计证据链。

---

## 11. 质量与审计清单（可直接用于答辩）

1. 模型名是否与算法实质一致。  
2. 是否有“召回即预测”的理论漏洞。  
3. 是否有 split 泄漏与 future 泄漏。  
4. 归一化策略是否模型化而非硬统一。  
5. 指标是否在原尺度计算。  
6. 实验是否可复现且日志完整。  
7. 测试是否覆盖泄漏、shape、指标、管线。  

---

## 12. 结论与演进路线

结论：检索式预测统一框架在理论自洽性、工程可审计性、实验复现性上可达到顶会级代码规范要求。  

建议下一阶段：
1. 增加 ANN 后端（Faiss/HNSW）统一适配层。  
2. 增加多变量距离学习与通道加权策略。  
3. 增加结果可视化与误差分解分析。  
4. 引入配置文件体系（YAML）和实验追踪系统。  

---
